name: "Bootstrap: 00 - Organization Structure"

on:
  pull_request:
    paths:
      - "bootstrap/00-org-structure/**"
      - ".github/workflows/bootstrap-00-org-structure.yml"
  push:
    branches: [main]
    paths:
      - "bootstrap/00-org-structure/**"

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  plan:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/terraform-reusable.yml
    with:
      working_directory: bootstrap/00-org-structure
      role_to_assume: ${{ vars.GHA_APPLY_ROLE_ARN }}
      oidc_provider_arn: ${{ vars.OIDC_PROVIDER_ARN }}
      terraform_action: plan

  apply:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: ./.github/workflows/terraform-reusable.yml
    with:
      working_directory: bootstrap/00-org-structure
      role_to_assume: ${{ vars.GHA_APPLY_ROLE_ARN }}
      oidc_provider_arn: ${{ vars.OIDC_PROVIDER_ARN }}
      terraform_action: apply
