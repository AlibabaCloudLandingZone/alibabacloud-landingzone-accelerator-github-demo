This is a leaf Terraform stack under `stacks/11-log-archive/` that currently contains only scaffolding and provider/backend wiring; no resources are declared yet. Internal layout follows the standard Terraform stack shape: `main.tf` holds the future `module "log_archive" { source = "../../modules/lza/components/log-archive" }` call (commented as TODO), `providers.tf` configures the `alicloud` provider with cross-account role assumption via `assume_role`, `variables.tf` exposes `region` (default `cn-hangzhou`) and the required `spoke_role_arn`, `outputs.tf` is a placeholder for SLS project/logstore identifiers, and `versions.tf` pins `terraform >= 1.5` and `aliyun/alicloud >= 1.262.1` while declaring an OSS backend keyed at `stacks/11-log-archive/terraform.tfstate` with Tablestore-based locking. Dependency direction is outward only — this stack depends on the shared `modules/lza/components/log-archive` component once implemented, and on CI/CD to supply `TF_VAR_spoke_role_arn`.